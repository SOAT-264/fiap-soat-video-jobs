apiVersion: v1
kind: Service
metadata:
  name: job-api-service
  namespace: video-processor
spec:
  $patch: replace
  selector:
    app: job-api-service
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
      nodePort: 32514
